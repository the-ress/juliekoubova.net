'use strict';

var chai       = require('chai')
  , Metalsmith = require('metalsmith')
  , myth       = require('..');

chai.should();

describe('metalsmith-myth', function () {
  it('should do stuff', function (done) {
    Metalsmith('test/fixtures')
  });
});
